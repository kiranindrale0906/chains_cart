
<?php

defined('BASEPATH') OR exit('No direct script access allowed');

class Migration_alter_table_rope_chian_bom_settings extends CI_Model {

  public function up()
  {
//    $this->db->query("ALTER TABLE `rope_chain_bom_settings` ADD `wt_per_inch` DECIMAL(10, 4) NOT NULL, ADD `cap_per_chain_wt` DECIMAL(10, 4) NOT NULL, ADD `iron_per_inch` DECIMAL(10, 4) NOT NULL, CHANGE `strip_weight` `strip_weight` DECIMAL(10,4) NOT NULL, CHANGE `langari_weight` `langari_weight` DECIMAL(10,4) NOT NULL;");
  }
}
